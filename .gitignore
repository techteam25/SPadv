*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
*debug*
*release*

# The instructions in README.md for building a locally signed APK include 
# putting these files in the root directory. We don't want them checked in.
/keystore.properties
/*.jks
app/local.properties

# Local temp / scratch (e.g. tutorials, drafts)
/temp

# Local env (e.g. GOOGLE_TRANSLATE_API_KEY for sync script) — do not commit
.env
